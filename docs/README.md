# Screenshots

The main README references three captures, expected here:

- `card-dark.png`: full card, several lines, dark theme (badges, departures board, map with vehicles)
- `focus-popup.png`: a tracked vehicle with its popup (badge, next stop, speed)
- `editor.png`: the visual editor (global options + per-line blocks)

Export them at their natural card width (about 450 to 900 px wide); no personal information should be visible.
